server:
  port: 80

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/world
    username: root
    password: root

mybatis-plus:
  mapper-locations: /mybatis/*.xml
  type-aliases-package: com.yuxin.*.domain
